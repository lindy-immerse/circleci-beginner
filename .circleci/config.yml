
version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.16-browsers
    steps:
      - checkout
      - run:
          name: "Create New Files"
          command: |
            pushd scripts/data
            echo "Hello!" > "some_file.js"
            popd
      - persist_to_workspace:
          root: scripts
  deploy:
    docker:
      - image: circleci/node:10.16-browsers
    steps:
      - checkout
      - run: echo "BOOM!"

workflows:
  version: 2
  build_test:
    jobs:
      - build
      - deploy:
          requires:
            - build



