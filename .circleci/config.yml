
version: 2
jobs:
  build:
    docker:
      - image: lindybritsimmerse/my-first-repo:myimage
    steps:
      - setup_remote_docker
      - restore_cache:
          keys:
            - v1-{{ .Branch }}
          paths:
            - /cache/app.tar
      - run:
          name: "Generate Test File"
          command: echo "Boom test!" > test-$(date +%s).txt
#      - run:
#          name: Install dependencies
#          command: |
#            apk add --no-cache \
#              py-pip=9.0.0-r1
#            pip install \
#              docker-compose==1.12.0 \
#              awscli==1.11.76
#      - run:
#          name: Load Docker image layer cache
#          command: |
#            set +o pipefail
#            docker load -i /caches/app.tar | true
#      - run:
#          name: Build application Docker image
#          command: |
#            docker build --cache-from=app -t app .
#      - run:
#          name: Save Docker image layer cache
#          command: |
#            mkdir -p /caches
#            docker save -o /caches/app.tar app
#      - save_cache:
#          key: v1-{{ .Branch }}
#          paths:
#            - /caches/app.tar
##  test:
#    docker:
#      - image: docker:17.05.0-ce-git
#    steps:
#      - setup_remote_docker
#      - run:
#          name: "Test"
#          command: echo "Boom!"


workflows:
  version: 2
  build_test:
    jobs:
      - build
#      - test:
#          requires:
#            - build
